import type { Metadata } from "next";
import { ContactForm } from "@/components/contact-form";
import { getContactConfig } from "@/lib/contact-config";

export const dynamic = "force-dynamic";
export const metadata: Metadata = { title: "Request a project review", description: "Start a Kepler Dev project review with the workflow or product problem that needs attention." };
export default async function ContactPage({ searchParams }: { searchParams: Promise<{ offer?: string }> }) { const { offer } = await searchParams; const config = getContactConfig(); return <section className="section route-hero"><div className="shell contact-layout"><div><p className="eyebrow">Project review</p><h1>Start with the workflow that needs to work better.</h1><p className="lead">Share the current problem, the outcome you need, and anything that gives useful context. This form is designed to make the first conversation more productive.</p></div><div className="contact-card">{config.enabled ? <ContactForm defaultOffer={offer || ""} fallbackEmail={config.fallbackEmail} /> : <section className="contact-unavailable" aria-labelledby="contact-unavailable-title"><h2 id="contact-unavailable-title">Online project enquiries are not available at the moment.</h2><p>Kepler Dev will only open this form once its delivery route and verified fallback are configured.</p>{config.fallbackEmail && <p>For a project review, use the verified fallback: <a href={`mailto:${config.fallbackEmail}`}>{config.fallbackEmail}</a>.</p>}</section>}</div></div></section>; }
